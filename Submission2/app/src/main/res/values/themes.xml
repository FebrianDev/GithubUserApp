<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.GithubApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black_primary</item>
        <item name="colorPrimaryVariant">@color/black_primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/black_primary</item>
        <item name="colorSecondaryVariant">@color/black_primary</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="TextDefault" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/customColorPrimary</item>
        <item name="android:textSize">14sp</item>
    </style>

    <color name="customColorPrimary">@color/black_primary</color>
    <color name="bgColorPrimary">@color/white_primary</color>

    <color name="customColorPrimarySecond">@color/white_primary</color>

</resources>